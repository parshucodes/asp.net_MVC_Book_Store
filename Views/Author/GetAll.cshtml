@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<MyBookStore.Models.Domain.Author>
<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" asp-action="Add" asp-controller="Author">Add More</a>
        </div>
    </div>
    <div class="col-md-6">
        <h3>Update Author</h3>
        <table class="table table-stripped tabe-bordered table-dark">
            <thead>
                <tr>
                    <th>AuthorName</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.AuthorName</td>
                        <td>
                            <a class="btn btn-success" href="/Author/Update?id=@item.Id">Edit</a>
                            @*<a class="btn btn-success" asp-action="Edit" asp-controller="Author" asp-route-id="@item.Id">Edit</a>*@
                            <a class="btn btn-danger" onclick="return window.confirm('Are you sure?')" href="/Author/Delete?id=@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
